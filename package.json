{
  "name": "l10n-tools",
  "version": "0.0.1",
  "description": "",
  "main": "dist/index",
  "scripts": {
    "prepare": "babel --source-maps true src --out-dir dist/",
    "watch": "babel -w --source-maps true src --out-dir dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Beren <beren.song@vonvon.me>",
  "license": "ISC",
  "bin": {
    "l10n": "./dist/l10n-cli.js"
  },
  "files": [
    "src",
    "dist"
  ],
  "dependencies": {
    "command-exists": "^1.2.2",
    "commander": "^2.14.1",
    "glob": "^7.1.2",
    "glob-promise": "^3.3.0",
    "i18next-conv": "^6.0.0",
    "jsonfile": "^4.0.0",
    "shelljs": "^0.8.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  }
}
